@model Admin
@{
    ViewData["Title"] = "Index";
}

<h1>Branch List</h1>

<table class=" table table-striped table-bordered table-hover">
    <thead>
         <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Address</th>
            <th>Salary</th>
            <th>Image</th>
            <th>More Details</th>
         </tr>
    </thead>
<tbody>
   @*  @foreach(var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Address</td>
                <td>@item.Salary</td>
                <td><img src="/Image/@item.Image" alt="Instructor Image" style="max-width: 70px; max-height: 70px;"></td>
                <td><a href="@Url.Action("Details","Instructor", new { id = item.Id })">View Details</a></td>
            </tr>
        } *@
    </tbody>
</table>


